# 📋 Crypto Screener — План Разработки и Трекинг

Этот документ служит основным чек-листом и архитектурным гайдом для разработки. Он обновляется по мере выполнения задач.

---

## 🛠 Технический Стек (Frontend MVP)
- **Core:** Next.js 14+ (App Router)
- **Language:** TypeScript
- **UI Framework:** Chakra UI (v2 или v3, уточнить)
- **State Management:** Zustand
- **Data Fetching:** TanStack Query (React Query)
- **Charts:** TradingView Lightweight Charts (для производительности в списках) / TradingView Widget (для детального)
- **TMA Integration:** @twa-dev/sdk
- **Icons:** Lucide React / React Icons

---

## 📅 Этап 1: Инициализация и UI MVP
*Цель: Полностью рабочий интерфейс на моковых данных, готовый к подключению API.*

### 1.1 Базовая настройка
- [ ] Инициализация Next.js проекта (Clean install).
- [ ] Настройка линтеров и форматирования (ESLint, Prettier).
- [ ] Установка и настройка Chakra UI (Custom Theme, Dark Mode default).
- [ ] Настройка алиасов путей (`@/components`, `@/hooks`, etc.).
- [ ] Подключение шрифтов (Inter/Roboto или системные).

### 1.2 Архитектура и Лэйаут
- [ ] Создание Root Layout (Mobile wrapper, макс. ширина).
- [ ] Реализация Bottom Navigation (Screener, Watchlist, Alerts, Profile).
- [ ] Настройка навигации в Next.js (App Router).

### 1.3 Главный экран (Screener)
- [ ] Компонент карточки монеты (Свернутый вид: Цена, 24h%, Sparkline).
- [ ] Компонент карточки монеты (Развернутый вид: RSI, MACD, кнопки действий).
- [ ] Реализация списка с виртуализацией (для производительности при 50+ монетах).
- [ ] Модальное окно фильтров (UI каркас).

### 1.4 Детальный экран (Coin Detail)
- [ ] Роутинг на страницу монеты (`/coin/[id]`).
- [ ] Интеграция графика TradingView.
- [ ] Блок основных метрик и индикаторов.

### 1.5 Второстепенные экраны
- [ ] **Watchlist:** Список избранного (UI).
- [ ] **Profile:** Настройки темы, заглушка профиля.
- [ ] **Alerts:** Заглушка (Coming Soon state).

---

## 📅 Этап 2: Логика и Мок-данные
- [ ] Создание генератора моковых данных (Mock Factory для монет).
- [ ] Настройка Zustand Store (хранение Watchlist локально, состояние фильтров).
- [ ] Реализация логики фильтрации (на клиенте для MVP).

---

## 💡 Идеи и Предложения (Для обсуждения)

1.  **Виртуализация списков (Virtualization):**
    *   *Зачем:* Скринеры часто содержат сотни элементов. Рендеринг всего DOM сразу "убьет" производительность на мобильных.
    *   *Решение:* Использовать `react-window` или `tanstack/virtual` для рендеринга только видимых элементов.

2.  **Легковесные графики в списке:**
    *   *Зачем:* Полноценный виджет TradingView тяжелый.
    *   *Решение:* В списке (Screener) использовать библиотеку `lightweight-charts` (canvas) или простые SVG (sparklines) для мини-графиков, а полный виджет — только на детальной странице.

3.  **Skeleton Loading:**
    *   *Зачем:* UX стандарт.
    *   *Решение:* Вместо спиннеров показывать "скелет" интерфейса при загрузке данных.

4.  **Haptic Feedback:**
    *   *Зачем:* Ощущение нативного приложения.
    *   *Решение:* Использовать `Telegram.WebApp.HapticFeedback` при нажатии на кнопки (лайк, фильтр) и при скролле (опционально).

5.  **PWA возможности:**
    *   *Зачем:* Кэширование ассетов.
    *   *Решение:* Настроить manifest и service worker, чтобы приложение грузилось мгновенно даже при плохом интернете внутри Telegram.

---

## 📝 Журнал изменений
- **19.01.2026:** Создан план разработки.
