# 🤖 AI Agent Context: Crypto Screener (TWA)

## 📌 Обзор проекта
Профессиональный крипто-скринер для Telegram (TWA). 
**Цель:** Реалтайм мониторинг 450+ пар с Binance, технический анализ и графики.

## 🛠 Технологический стек
- **Frontend:** Next.js 14 (App Router), Chakra UI, TanStack Query v5, Lightweight Charts.
- **Backend:** Python 3.10 (FastAPI), CCXT (Async/Pro), TimescaleDB (Postgres 14).
- **Инфраструктура:** Docker Compose, VPS (8 vCPU, 16GB RAM).

## ⚡️ Текущее состояние & Оптимизации
1. **Real-time:** `streamer.py` собирает данные через WebSocket Binance. Запись в БД пачками по 1000 строк (раз в 1 сек) для снижения нагрузки.
2. **Performance:** 
   - Включен Gzip на бэкенде.
   - На фронтенде реализован Prefetching (Intersection Observer) и прогрессивная загрузка графиков (200 -> 3000 свечей).
   - Технические индикаторы считаются на **клиенте** (`lib/indicators.ts`) для мгновенной реакции при смене ТФ.
3. **Data:** Спарклайны предрассчитываются в `worker.py` и хранятся в `coin_status` как JSON. Market Cap синхронизируется с CoinMarketCap API через `sync_cmc.py`.

## 🛡 Безопасность (Критично!)
- **Инцидент (22.01.2026):** Сервер был заражен майнером `kdevtmpfsi`. 
- **Принятые меры:** Вирус удален, процессы убиты. Порт БД (5432) **ЗАКРЫТ** для внешнего мира. База доступна только внутри Docker-сети.
- **БД:** Пароль изменен в `.env`. ВНИМАНИЕ: Пользователь `postgres` в БД может иметь ограниченные права (не SUPERUSER) из-за вируса, но система работает через `trust` внутри сети.

## 🎯 Следующая задача
**Переход на архитектуру WebSocket + Redis.**
- Сейчас фронтенд использует HTTP Polling (1 сек). Нужно заменить на WebSockets.
- План: `streamer.py` -> Redis Pub/Sub -> `ws_server` (FastAPI) -> Client.

## 🔑 Ключи и доступы
- **CMC API Key:** В `.env` на сервере.
- **Postgres:** Данные в `.env`. Порты закрыты.
- **Server IP:** 45.248.37.175
